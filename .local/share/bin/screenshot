#!/bin/bash

selection=$(slurp)

if [ -n "$selection" ]; then
	grim -g "$selection"
	grim -g "$selection" - | wl-copy
	notify-send "Screenshot saved"
else
	notify-send "Screenshot cancelled"
fi
echo $selection
